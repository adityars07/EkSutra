apiVersion: v1
kind: Service
metadata:
  name: govtIP-service
  namespace: govtIP
spec:
  selector:
    app: govtIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
